# Application Name
spring.application.name=InventoryManagementApplication

# Database Configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/inventory_database}
spring.datasource.username=${DB_USER:the_inventory_user_1}
spring.datasource.password=${DB_PASSWORD:must2004$}
server.port=${SERVER_PORT:8080}

spring.datasource.driver-class-name=org.postgresql.Driver


spring.jpa.properties.hibernate.format_sql=true


spring.jpa.defer-datasource-initialization=true


spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# OpenAPI/Swagger Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=afzunov12@gmail.com
spring.mail.password=Must2788$
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Notification Configuration
notification.email.recipient=afzunov12@gmail.com

